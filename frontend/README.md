# frontend/

Not scaffolded yet — deliberately left as a placeholder rather than running
`create-next-app` unasked, since that pulls a full dependency tree. Reference:
`fce-weather-insurance/frontend` (Next.js + wagmi, MetaMask on Coston2, proxies
TEE `/info` and `/action/result` through Next API routes to avoid browser CORS).
